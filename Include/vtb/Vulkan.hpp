#pragma once

#include <vector>

#include "Config.hpp"
#include "GetVector.hpp"
#include "Debug.hpp"

VTB_BEGIN

using ShaderData = std::vector<char>;

struct Device;
struct Buffer;
struct SwapChain;
struct Semaphore;
struct Image;
struct ImageView;

VTB_END
